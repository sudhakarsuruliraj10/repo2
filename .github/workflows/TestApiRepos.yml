name: Call a reusable workflow and use its outputs

on:
  workflow_dispatch:
  repository_dispatch:
   

jobs:
  job1:
   runs-on: ubuntu-latest
   outputs:
      output1: ${{ steps.trigger-step2.outputs.output1 }}
   steps:    
     - name: Fetch remote job output
       run: |
           curl -H "Authorization: Bearer ${{ secrets.ACTIONS_KEY }}" \
                https://api.github.com/repos/sudhakarsuruliraj10/repo2/actions/runs/${{ steps.trigger-step.outputs.workflow-id }}/jobs \
                 | jq '.jobs[] | select(.name == "example_job") | .steps[] | .outputs'
